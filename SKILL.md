---
name: developing-skills
description: 从需求、专业资料或执行记录开发 Agent Skill，或使用已标注案例自动评测、八层归因、修改候选并迭代选择最佳版本。适用于复杂专业 Skill 的方法建立、能力分配和证据化改进；单纯安装、启停、格式修改交给现有 skill-creator。
---

# Skill 开发工作流

把用户的业务目标推进为可执行、可检验的 Skill。先判断关键缺口，再选择所需活动；不要把阶段名称变成每次都要填写的文档。

## 进入任务

读取已有目标、资料、授权和实际文件。选择模式：新建（需求起步）、提炼（案例或演示起步）、改进（现有 Skill 起步）、标注优化（案例及真值起步）。另选深度：方法或行为边界变化走完整路径；影响明确的局部修正只检查相关行为。已有决定不重复询问；只有无法推断、会实质改变方案的未知才澄清，先完成不依赖答案的工作。

用户提供已标注案例或要求自动优化时，读 [标注案例自动优化](references/labeled-case-optimization.md)、[标注与结果格式](references/eval-schema.md) 和 [Agent 运行时适配协议](references/runtime-adapter.md)。需要连接实际 CodeAgent CLI 时再读 [CodeAgent 适配器配置](references/codeagent-adapter-config.md)。默认执行 L2：允许自动修改隔离的候选副本并多轮评测，不覆盖或发布正式 Skill。只有用户明确授权且目标环境支持，才晋升正式版本。

高保证自动优化、留出治理、执行恢复、grader 校准、真实专业案例验收、统一 Viewer 或晋升准备时，再读 [生产保证、恢复与治理](references/production-assurance.md)。候选变化后自动刷新追踪与一致性证据；刷新失败只阻断该候选，不得沿用旧证据。恢复使用带 lineage 的后继工作区；源工作区已消费留出时必须换新留出集。统一晋升包只供人工裁决，不执行发布。

继续旧任务时核对工作记录与实际候选；输入、方法、工具或评分发生变化，标记受影响结果需重验。没有原始证据的“上次通过”只能作为待核实记录。

新建专业 Skill，或目标、触发边界、方法、能力依赖、风险、输出契约或 Eval 判据发生实质变化时，初始化前必须读 [设计框架与门禁](references/design-framework.md)，输出并校验 `skill-design.md`。由本 Skill 负责设计内容；确认关键决策后再调用目标环境的 skill-creator。局部修正可跳过完整设计，但要说明为何不影响上述边界。

复用目标环境已有 skill-creator 的初始化、格式验证和保存机制；业务方法与评测由本 Skill 组织。用户要求或环境提供 Anthropic `skill-creator` 时，先读 [Anthropic skill-creator 集成](references/anthropic-skill-creator-integration.md)：宿主原生调用它做通用创作审查，脚本适配层直接调用校验、打包、Viewer 和 Claude description 优化。记录真实提供者与版本；Anthropic 不可用时可使用当前宿主的原生 skill-creator，但不得混称。没有该能力时按当前环境支持的格式生成候选，并如实说明可调用状态。不要从通用文件格式推断平台工具已经存在。

安装或分发本 Skill、检查推荐依赖，或首次启用 Anthropic provider 时，读 [推荐依赖安装](references/dependency-installation.md)。默认推荐并选择固定版本的 Anthropic `skill-creator`，但实际网络下载前必须向用户显示来源、commit、许可证和私有目标目录并取得明确确认；拒绝时跳过，不影响主 Skill。不得把私有 provider 注册成第二个全局同名 Skill，也不得自动跟随上游更新。

## 推进开发

1. **定义可观察结果。** 明确使用者、触发范围、输入输出、关键边界和完成标准。至少想清一个正常任务和一个会改变决策的边界；小改动不强制新增用例。
2. **建立方法。** 从资料或案例提取“情境 → 所需证据 → 判断条件 → 行动 → 结果检查”。区分来源支持、案例观察、暂定假设与已执行验证。遇到专业方法不清、提炼经验或冲突材料，读 [方法与能力设计](references/method-design.md)。不要把一次成功中的偶然操作变成通用规则。
3. **核实能力。** 决策指导写入 Skill；大量知识放参考资料；稳定机械操作用脚本；系统访问用已存在的工具；隔离与授权属于运行环境。对每个关键分支核实依赖状态与失败语义。缺少可选能力只阻断依赖它的分支，替代或模拟需标明。
4. **通过设计保证门禁。** 对新建、实质更新或高风险任务，按风险选择 standard/high-assurance 深度，用稳定 section ID 和结构化设计项写入 `skill-design.md`。确定性结构校验后，由不同于修改者的审查身份评审需求、方法、能力、评价与安全；再生成精确绑定 `design_id` 的外部 approval。设计正文不能自我声明已批准。局部低风险修正可走 lightweight，但不得借此规避受影响的关键设计项。
5. **初始化并构建最小候选。** 将已审批设计交给实际可用的 skill-creator 初始化；若提供者没有可调用初始化脚本，生成 provider handoff，宿主原生调用后保存外部绑定记录。实现后逐个把设计项映射到真实文件/已验证外部能力和 Eval expectation；关键项必须双向覆盖。再做独立 conformance review，只有实现绑定通过才进入正式晋升。优先一条完整业务路径及关键异常；入口只写会改变行为的指导。按需增加资源，不预建多 Agent 架构、通用执行引擎或固定数量文件。保存可比较版本，检查引用、依赖，并实际运行新增脚本。
6. **执行与评判。** 对复杂或行为变化的 Skill，先读 [评测与归因](references/evaluation.md)，冻结设计、候选和判据，再用真实任务执行。检查实际产物及副作用，不能用格式校验替代行为验证。区分辅助代码、目标 Skill、开发元 Skill 三层证据。独立执行只在环境支持且授权允许时使用；缺失时自查不冒充独立测试。比较时同轮启动候选与基线，保持任务、输入、权限、模型和可比预算一致；能可靠重复时才报告方差。
7. **按失败修正。** 依据具体输入、输出和影响，定位需求、方法、知识、检索、工具、运行时、指令或评分层。修改最小责任部分；requirement 失败返回设计门禁。检查同类缺陷和一个防止修正过度的相反案例。评分层变化后重评所有比较版本；向修改器暴露过的留出样例转为回归。候选内容变化后，用原批准设计和追踪映射重新核验实际实现，并由独立 reviewer 生成新 conformance 证据；完整重跑训练/回归后才允许访问留出。验证足够后交付，避免为增加数字重复执行。
8. **核实交付。** 按环境要求保存可调用 Skill，核对设计、approval、追踪、conformance、候选、案例集与验证对象的标识一致。自动修改后旧实现证据不随文字沿用，最终候选必须重新验证。若调用 Anthropic 打包器，只把它作为结构与打包证据；正式晋升仍执行本 Skill 的回归/留出门禁并由用户批准。明确已实现、实际验证、未执行与剩余限制；只在测量支持时声明质量或效率提升。

## 保留可恢复的工作证据

复杂或跨轮工作用一个紧凑工作记录串起目标、方法、依赖、候选、问题、验证和下一行动；按需采用 [工作记录](references/work-record.md)，不机械生成七份表格。将开发记录与目标 Skill 的运行资源分开保存。

每条重要结论指向可检查产物。记录可观察理由和证据，不索取模型内部思维链。用户授权支持范围内连续执行，但不扩展到未授权的发布、生产修改或对外通信。平台审批与存储要求遵循当前环境，不在此硬编码。

结束时说明交付物、验证结果和一个最有价值的后续方向。未完成关键验收时说明具体阻断及已完成部分，不能只说“流程已跑通”。
